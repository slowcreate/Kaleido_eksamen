---
// Ingen frontmatter JS nødvendigt
---

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const countdown = document.getElementById('countdown');
    const target = new Date('2025-06-20T14:00:00Z');

    function updateCountdown() {
      const now = new Date();
      const diff = target.getTime() - now.getTime();

      const selectors = ['days', 'hours', 'minutes', 'seconds'];

      if (diff <= 0) {
        selectors.forEach(type => {
          const el = document.querySelector(`[data-type="${type}"]`);
          if (el) el.textContent = '0';
        });
        return;
      }

      const days = Math.floor(diff / (1000 * 60 * 60 * 24));
      const hours = Math.floor((diff / (1000 * 60 * 60)) % 24);
      const minutes = Math.floor((diff / (1000 * 60)) % 60);
      const seconds = Math.floor((diff / 1000) % 60);

      const values = { days, hours, minutes, seconds };

      for (const [type, value] of Object.entries(values)) {
        const el = document.querySelector(`[data-type="${type}"]`);
        if (el) el.textContent = String(value); // 💡 konvertering til string
      }
    }

    updateCountdown();
    setInterval(updateCountdown, 1000);
  });
</script>


<div class="text-center my-5xl">
  <h2 class="text-primary-50 text-xl sm:text-2xl md:text-4xl">
    We <span class="uppercase text-primary-50 text-3xl sm:text-ml md:text-6xl lg:text-7xl">can't wait</span> to see you
  </h2>

  <div id="countdown" class="flex flex-row justify-center gap-2 sm:gap-4 md:gap-5 mt-6">
    <div class="flex flex-col items-center px-3 py-4 sm:px-5 sm:py-6 md:p-8 lg:p-10 mt-sm bg-secondary-500 rounded-2xl text-primary-50 unbounded w-[60px] sm:w-[70px] md:w-4xl">
      <span class="bronatte text-3xl sm:text-5xl md:text-7xl lg:text-8xl text-primary-50" data-type="days">0</span>
      <span class="text-xs sm:text-sm md:text-base">days</span>
    </div>
    <div class="flex flex-col items-center px-3 py-4 sm:px-5 sm:py-6 md:p-8 lg:p-10 mt-sm bg-secondary-500 rounded-2xl text-primary-50 unbounded w-[60px] sm:w-[70px] md:w-4xl">
      <span class="bronatte text-3xl sm:text-5xl md:text-7xl lg:text-8xl text-primary-50" data-type="hours">0</span>
      <span class="text-xs sm:text-sm md:text-base">hours</span>
    </div>
    <div class="flex flex-col items-center px-3 py-4 sm:px-5 sm:py-6 md:p-8 lg:p-10 mt-sm bg-secondary-500 rounded-2xl text-primary-50 unbounded w-[60px] sm:w-[70px] md:w-4xl">
      <span class="bronatte text-3xl sm:text-5xl md:text-7xl lg:text-8xl text-primary-50" data-type="minutes">0</span>
      <span class="text-xs sm:text-sm md:text-base">min</span>
    </div>
    <div class="flex flex-col items-center px-3 py-4 sm:px-5 sm:py-6 md:p-8 lg:p-10 mt-sm bg-secondary-500 rounded-2xl text-primary-50 unbounded w-[60px] sm:w-[70px] md:w-4xl">
      <span class="bronatte text-3xl sm:text-5xl md:text-7xl lg:text-8xl text-primary-50" data-type="seconds">0</span>
      <span class="text-xs sm:text-sm md:text-base">sec</span>
    </div>
  </div>
</div>

